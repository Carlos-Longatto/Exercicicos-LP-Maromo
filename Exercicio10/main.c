#include <stdio.h>
#include <stdlib.h>
#define L 4
#define C 2

int main()
{
    int a[L][C], b[L][C], c[L][C], i, j;

    printf("Vanos entrar com valores para a matriz A, depois a B e no final fares a soma das 2 matrizes\n\\n");

    for (i=0; i<L; i++){
        printf("Digite os valores da matriz a: ");
        scanf("%d", &a[i]);
            for (i=0; i<C; i++){
        printf("Digite os valores da matriz a: ");
        scanf("%d", &a[i]);
            }
    }
    return 0;
}
